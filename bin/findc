#!/usr/bin/env perl

use strict;
use warnings;

my @args = map { $_ eq '-print'
    ? ('-printf', q{\033[32m==> %p <==\033[0m\n})
    : $_
} @ARGV;

print "Running 'find @args\n\n'";
exec {'find'} @args;
